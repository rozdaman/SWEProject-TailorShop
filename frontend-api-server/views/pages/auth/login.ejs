<!DOCTYPE html>
<html lang="tr">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Giriş Yap - Tailor</title>
    <!-- Bootstrap CSS -->
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css"
      rel="stylesheet"
    />
    <link
      rel="stylesheet"
      href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.0/font/bootstrap-icons.css"
    />

    <style>
      body,
      html {
        height: 100%;
        font-family: "Segoe UI", Tahoma, Geneva, Verdana, sans-serif;
      }

      .login-container {
        min-height: 100vh;
        display: flex;
      }

      /* SOL TARAF: GÖRSEL */
      .image-side {
        /* KENDİ FOTOĞRAFINI EKLEMEK İÇİN:
               1. Fotoğrafını 'public/images' klasörüne yükle.
               2. Aşağıdaki url parantezi içini güncelle. Örn: url('/images/login-bg.jpg')
            */
        background: url("/images/login.jpg");
        position: relative;
        display: none; /* Mobilde gizle */
      }

      .image-overlay {
        position: absolute;
        top: 0;
        left: 0;
        width: 100%;
        height: 100%;
        background: linear-gradient(
          to bottom,
          rgba(0, 0, 0, 0.2),
          rgba(0, 0, 0, 0.7)
        );
        display: flex;
        flex-direction: column;
        justify-content: flex-end;
        padding: 60px;
        color: white;
      }

      /* SAĞ TARAF: FORM */
      .form-side {
        flex: 1;
        display: flex;
        align-items: center;
        justify-content: center;
        background-color: #ffffff;
        padding: 40px;
        position: relative;
      }

      .form-wrapper {
        width: 100%;
        max-width: 450px;
      }

      .brand-logo {
        font-size: 2rem;
        font-weight: 800;
        letter-spacing: 2px;
        color: #2c3e50;
        text-decoration: none;
        display: inline-block;
        margin-bottom: 40px;
      }

      .form-control {
        padding: 12px 15px;
        border-radius: 10px;
        border: 1px solid #dee2e6;
        background-color: #f8f9fa;
        font-size: 1rem;
      }
      .form-control:focus {
        background-color: #fff;
        border-color: #0d6efd;
        box-shadow: 0 0 0 4px rgba(13, 110, 253, 0.1);
      }

      .btn-login {
        padding: 14px;
        font-weight: 600;
        border-radius: 10px;
        font-size: 1rem;
        background-color: #2c3e50;
        border: none;
        transition: 0.3s;
        width: 100%;
      }
      .btn-login:hover {
        background-color: #1a252f;
        transform: translateY(-2px);
        box-shadow: 0 5px 15px rgba(44, 62, 80, 0.3);
      }

      .social-btn {
        border: 1px solid #e9ecef;
        background: #fff;
        color: #495057;
        padding: 12px;
        border-radius: 10px;
        font-weight: 500;
        transition: 0.2s;
        text-decoration: none;
        display: flex;
        align-items: center;
        justify-content: center;
        font-size: 0.95rem;
      }
      .social-btn:hover {
        background: #f8f9fa;
        border-color: #dee2e6;
        color: #000;
        transform: translateY(-1px);
      }

      .divider {
        display: flex;
        align-items: center;
        text-align: center;
        margin: 30px 0;
        color: #adb5bd;
        font-size: 0.85rem;
        font-weight: 500;
        text-transform: uppercase;
        letter-spacing: 1px;
      }
      .divider::before,
      .divider::after {
        content: "";
        flex: 1;
        border-bottom: 1px solid #e9ecef;
      }
      .divider:not(:empty)::before {
        margin-right: 1em;
      }
      .divider:not(:empty)::after {
        margin-left: 1em;
      }

      .back-link {
        position: absolute;
        top: 30px;
        right: 30px;
        text-decoration: none;
        color: #6c757d;
        font-size: 0.9rem;
        transition: 0.2s;
      }
      .back-link:hover {
        color: #2c3e50;
      }

      /* Tablet ve Masaüstü */
      @media (min-width: 992px) {
        .image-side {
          display: block;
          width: 55%;
        }
        .form-side {
          width: 45%;
        }
      }
    </style>
  </head>
  <body>
    <div class="container-fluid p-0 login-container">
      <!-- SOL: Görsel Alanı -->
      <div class="image-side">
        <div class="image-overlay">
          <h1 class="display-4 fw-bold mb-3">Tarzınızı Yansıtın.</h1>
          <p class="fs-5 opacity-75">
            Kişiye özel dikim ve eşsiz kumaş koleksiyonları ile fark yaratın.
            Hesabınıza giriş yaparak alışverişe devam edin.
          </p>
        </div>
      </div>

      <!-- SAĞ: Form Alanı -->
      <div class="form-side">
        <a href="/" class="back-link"
          ><i class="bi bi-house me-1"></i> Ana Sayfa</a
        >

        <div class="form-wrapper">
          <div class="text-center text-lg-start">
            <a href="/" class="brand-logo">TAILOR.</a>
            <h3 class="fw-bold text-dark mb-2">Tekrar Hoşgeldiniz!</h3>
            <p class="text-muted mb-4">Lütfen bilgilerinizi giriniz.</p>
          </div>

          <!-- Hata Mesajı -->
          <% if (error) { %>
          <div
            class="alert alert-danger d-flex align-items-center mb-4 border-0 shadow-sm rounded-3 py-3"
            role="alert"
          >
            <i class="bi bi-exclamation-triangle-fill fs-5 me-3"></i>
            <div class="small"><%= error %></div>
          </div>
          <% } %>

          <!-- Kayıt Başarılı Mesajı -->
          <% if (typeof registered !== 'undefined' || (typeof query !==
          'undefined' && query.registered)) { %>
          <div
            class="alert alert-success d-flex align-items-center mb-4 border-0 shadow-sm rounded-3 py-3"
            role="alert"
          >
            <i class="bi bi-check-circle-fill fs-5 me-3"></i>
            <div class="small">
              Kayıt işlemi başarılı! Şimdi giriş yapabilirsiniz.
            </div>
          </div>
          <% } %>

          <form action="/auth/login" method="POST">
            <div class="mb-3">
              <label class="form-label small fw-bold text-muted">E-POSTA</label>
              <div class="input-group">
                <span class="input-group-text bg-light border-end-0 text-muted"
                  ><i class="bi bi-envelope"></i
                ></span>
                <input
                  type="email"
                  class="form-control border-start-0 ps-0"
                  name="email"
                  placeholder="name@example.com"
                  required
                />
              </div>
            </div>

            <div class="mb-3">
              <label class="form-label small fw-bold text-muted">ŞİFRE</label>
              <div class="input-group">
                <span class="input-group-text bg-light border-end-0 text-muted"
                  ><i class="bi bi-lock"></i
                ></span>
                <input
                  type="password"
                  class="form-control border-start-0 ps-0"
                  name="password"
                  placeholder="******"
                  required
                />
              </div>
            </div>

            <div class="d-flex justify-content-between align-items-center mb-4">
              <div class="form-check">
                <input
                  class="form-check-input"
                  type="checkbox"
                  id="rememberMe"
                />
                <label
                  class="form-check-label small text-muted"
                  for="rememberMe"
                  >Beni Hatırla</label
                >
              </div>
              <a
                href="#"
                class="text-decoration-none small fw-semibold text-primary"
                >Şifremi Unuttum</a
              >
            </div>

            <button
              type="submit"
              class="btn btn-primary btn-login text-white mb-4"
            >
              Giriş Yap <i class="bi bi-arrow-right ms-2"></i>
            </button>
          </form>

          <div class="divider">veya</div>

          <div class="row g-2 mb-4">
            <div class="col">
              <a href="#" class="social-btn">
                <i class="bi bi-google text-danger me-2"></i>
                <span class="d-none d-sm-inline">Google</span>
              </a>
            </div>
            <div class="col">
              <a href="#" class="social-btn">
                <i class="bi bi-facebook text-primary me-2"></i>
                <span class="d-none d-sm-inline">Facebook</span>
              </a>
            </div>
          </div>

          <p class="text-center text-muted small mb-0">
            Hesabınız yok mu?
            <a
              href="/auth/register"
              class="fw-bold text-primary text-decoration-none"
              >Hemen Kayıt Olun</a
            >
          </p>
        </div>
      </div>
    </div>
  </body>
</html>
