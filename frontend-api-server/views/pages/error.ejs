<%- include('../partials/header') %>
<%- include('../partials/navbar') %>

<main class="container py-5">
    <div class="text-center py-5">
        <i class="bi bi-exclamation-triangle display-1 text-danger mb-4"></i>
        <h2 class="mb-3">Bir Hata Oluştu</h2>
        <p class="text-muted mb-4"><%= error || 'Beklenmeyen bir hata oluştu. Lütfen daha sonra tekrar deneyin.' %></p>
        <div class="d-flex justify-content-center gap-3">
            <a href="/" class="btn btn-primary">
                <i class="bi bi-house me-2"></i>Ana Sayfa
            </a>
            <button onclick="history.back()" class="btn btn-outline-primary">
                <i class="bi bi-arrow-left me-2"></i>Geri Dön
            </button>
        </div>
    </div>
</main>

<%- include('../partials/footer') %>
